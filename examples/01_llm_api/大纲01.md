第一周：LLM API
今天 Day 1

主课程：

看这几个章节

课程里的：

05. 通过 API 访问本地部署的大模型
https://www.bilibili.com/video/BV1Uu3v6VEg8/?spm_id_from=333.788.videopod.episodes&vd_source=f44d7e53f07d49299b1e1755d48b8633&p=6

但这里有个问题：这个章节是“本地模型 API”，而我们今天使用的是 DeepSeek API。

所以今天主要借它理解“程序如何通过 API 调模型”这个概念，真正的代码以 DeepSeek 官方文档为准。

DeepSeek 当前官方文档明确支持 OpenAI-compatible API，而且目前提供 deepseek-v4-flash、deepseek-v4-pro 等模型。

DeepSeek 官方文档｜首次调用 API

今天看多少？

不要看完整课程。

今天：

📺 视频

约 10～20 分钟

重点理解：

API Key
Base URL
Model
Messages
然后开始写代码

你之前已经使用过 DeepSeek，所以今天直接做。

创建：

ai-learning/
└── 01-llm-api/
    ├── .env
    ├── .gitignore
    └── main.py

安装：

pip install openai python-dotenv

.env：

OPENAI_API_KEY=你的key
OPENAI_BASE_URL=https://api.deepseek.com
MODEL_NAME=deepseek-v4-flash

DeepSeek 官方当前的 OpenAI-compatible Base URL 是 https://api.deepseek.com。

然后：

import os

from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI(
    api_key=os.getenv("OPENAI_API_KEY"),
    base_url=os.getenv("OPENAI_BASE_URL"),
)

response = client.chat.completions.create(
    model=os.getenv("MODEL_NAME"),
    messages=[
        {
            "role": "user",
            "content": "你好，请用一句话介绍一下你自己。",
        }
    ],
)

print(response.choices[0].message.content)

运行：

python main.py
今天第二个任务

把程序改成：

用户输入问题
       ↓
Python
       ↓
DeepSeek
       ↓
回答

也就是：

question = input("请输入问题：")

response = client.chat.completions.create(
    model=os.getenv("MODEL_NAME"),
    messages=[
        {
            "role": "user",
            "content": question,
        }
    ],
)

print(response.choices[0].message.content)

做到这里，今天就已经合格了。