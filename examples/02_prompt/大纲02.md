## Day 2｜Prompt 基础 + 结构化输出

今天是 **2026 年 9 月 2 日**。昨天已经完成或开始了 LLM API 调用，今天进入 **Prompt 和结构化输出**。目标不是背提示词技巧，而是学会让模型按你的要求稳定输出，给后面的日志分析器打基础。

### 今天看什么

主课程继续使用：

[《大模型应用开发教程》B站课程](https://www.bilibili.com/video/BV1Uu3v6VEg8?utm_source=chatgpt.com)

今天看这两节：

1. **「认知篇」01：什么是 AI 大模型开发**
    只看前半部分，约 5～8 分钟，重点理解 LLM 在应用中的位置。 
2. **「进阶篇」中与 Prompt / 提示词工程相关的章节**
    课程目录中如果看到“提示词工程、Prompt 模板、System Prompt、Few-shot”这些关键词，今天选择最接近的 1～2 节，总观看时间控制在 **20～25 分钟**。这套课程目前是 2026 年 7 月 28 日发布的，包含 Python、提示词工程、RAG、Agent 等内容；不要顺着自动连播继续往后看。 

如果你打开后发现目录名称和上面略有差异，以关键词为准，不要花时间翻完整个 99 集目录。

### 今天需要掌握的 4 个知识点

```
```

```
System Prompt：规定模型的身份、规则和输出要求
User Prompt：本次具体任务和输入数据
Few-shot：给模型看少量输入/输出示例
Structured Output：让模型返回固定 JSON 结构
```

你今天要理解的核心是：

```
```

```
普通提问：模型自由回答
结构化要求：模型输出可以被程序继续处理的数据
```

### 今天写什么代码

在昨天的 `01-llm-api` 目录下新增：

```
```

```
prompt_demo.py
```

先完成一个固定角色调用：

```
```

```
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI(
    api_key=os.getenv("OPENAI_API_KEY"),
    base_url=os.getenv("OPENAI_BASE_URL"),
)

response = client.chat.completions.create(
    model=os.getenv("MODEL_NAME"),
    messages=[
        {
            "role": "system",
            "content": (
                "你是一名资深 Java 后端故障分析工程师。"
                "回答必须简洁、分点，并区分现象、原因和建议。"
            ),
        },
        {
            "role": "user",
            "content": (
                "请分析下面的错误日志：\n"
                "HikariPool-1 - Exception during pool initialization"
            ),
        },
    ],
)

print(response.choices[0].message.content)
```

然后把提示词改成要求 JSON：

```
```

```
prompt = """
请分析下面的 Java 错误日志，只返回 JSON，不要添加 Markdown 代码块。
JSON 字段必须包含：
error_type、possible_causes、suggestions。
其中 possible_causes 和 suggestions 必须是数组。

错误日志：
HikariPool-1 - Exception during pool initialization
"""
```

先不要急着引入复杂框架，今天只观察模型是否能按要求返回类似：

```
```

```
{
  "error_type": "数据库连接池初始化失败",
  "possible_causes": [
    "数据库地址不可达",
    "用户名或密码错误"
  ],
  "suggestions": [
    "检查数据库连接配置",
    "测试网络连通性"
  ]
}
```

### 今天的小练习

准备 3 条日志，让程序分别分析：

```
```

```
HikariPool-1 - Exception during pool initialization
Communications link failure
Unable to connect to any servers
```

再做一个对比：同一条日志分别使用“普通提示词”和“JSON 输出提示词”，观察两次结果有什么区别。

### 预计耗时

-  看视频：20～25 分钟 
-  改代码：25～30 分钟 
-  对比测试与记录：10 分钟 

总计约 **1 小时**。

### 今天验收标准

今天完成下面 4 项就算达标：

-  能说明 `system` 和 `user` 的区别 
-  能让模型按固定角色回答 
-  能让模型输出固定字段 
-  能用 3 条日志测试结果 

今天不要看 RAG、Agent、LangGraph、MCP。明天继续做**结构化输出的程序解析**，把模型返回的 JSON 真正转换成 Python 对象。
在Day2是否要学agent tuning 